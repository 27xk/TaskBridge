<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="${allowBackup}"
        android:icon="@mipmap/ic_launcher"
        android:label="TaskBridge"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.TaskBridge"
        android:usesCleartextTraffic="${usesCleartextTraffic}">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
                <data android:mimeType="application/json" />
            </intent-filter>
        </activity>

        <receiver
            android:name=".widget.TodayTaskWidgetProvider"
            android:exported="true"
            android:label="@string/widget_label_blank">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/today_task_widget_info" />
        </receiver>

        <receiver
            android:name=".widget.WidgetActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.taskbridge.app.widget.action.COMPLETE" />
                <action android:name="com.taskbridge.app.widget.action.SNOOZE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".notification.ReminderReceiver"
            android:exported="false" />
    </application>
</manifest>
